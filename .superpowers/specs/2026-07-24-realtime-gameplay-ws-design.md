# x-server 实时玩法（WS 长连接）设计

> 范围：实时玩法（轻交互玩法）的架构设计。**独立里程碑，菜鸟期不落地**。本文档为 [架构层设计](./2026-07-24-server-architecture-design.md) 的延伸，复用其数据层（Redis 权威 + Redisson 分布式锁 `lock:player:{id}`）、认证 token 体系与非功能配套，不重复定义。

## 1. 定位与同步模型

- **轻交互玩法场景**：2~20 人实时可见、竞技协作/仲裁。
- **同步模型**：**状态同步为主**。服务器持有权威状态，客户端上报意图，服务器校验/推进后广播变更。不做帧同步、不引入确定性逻辑/追帧机制。
- **部署形态**：独立 Netty WS 进程，按房间路由（与无状态 HTTP 业务进程分离，实现故障隔离与流量隔离——实时玩法终究只是游戏中的一个玩法，不能让一个玩法拖垮整个系统）。

---

## 2. 网络通道与分流

采用 **"HTTP 负责常规数据，Netty 负责实时交互"** 的按需连撤机制：

```
[客户端 Unity]
       │
       ├─ (1) 登录/背包/装备/商城 (常规操作) ──────────────► [HTTP POST / Spring Boot MVC]
       │                                                       (短连接，处理完即断开)
       │
       ├─ (2) 点击“进入 2~20 人房间” ────────────────────────► [HTTP POST] 申请房间，获取 IP + RoomToken
       │
       ├─ (3) 建立 WebSocket 长连接 (携带 RoomToken) ───────► [嵌入式 Netty WS Server]
       │                                                       (原生 Netty Channel Pipeline)
       │
       ├─ (4) 场景内实时移动/战斗仲裁 ◄══════════════════════► [Netty WS Server]
       │                                                       (二进制 Protobuf 双向持续通信)
       │
       └─ (5) 结算/退出场景 ─────────────────────────────────► [Netty WS Server] 断开 WS，释放 Channel 资源

```

### 通道职责划分：

| 通道 | 承载业务 | 技术形态与线程模型 |
| --- | --- | --- |
| **HTTP 短连接** | 登录、数据读写、背包、商城等非实时操作 | POST + Protobuf；Spring MVC + Tomcat 自动挂载虚拟线程 |
| **WS 长连接** | 实时竞技、位置同步、玩法仲裁（仅进入玩法时建立） | 原生 Netty 监听专属端口 + Protobuf 解码；业务丢入虚拟线程池 |
| **gRPC** | 跨进程同步调用 | Protobuf 契约预留，菜鸟期只留在代码不部署 |

### 明确不做（YAGNI）：

* 菜鸟期不引入 Redis Pub/Sub / Stream 做跨进程广播与通知。
* 不预埋独立网关进程，成熟期拆进程时，客户端直接直连独立的战斗进程 Netty 端口。

---

## 3. 会话与断连（进程内）

- Netty Channel 进程内维护映射表（`Channel ↔ 玩家ID`）。
- **不做长重连恢复，保留短时挂起（Grace Period）**：
- WS 物理断开时， Session 不立即销毁，标记为 `SUSPENDED` 并保留 15 秒。
- 15 秒内重新握手成功直接恢复会话；超过 15 秒则彻底清除，下次重连重新拉全量状态，规避移动端网络闪断引发的全量数据拉取风暴。

* **留路方式**：抽象 `SessionManager` 接口，菜鸟期进程内实现，成熟期换 Redis/网关实现。

---

## 4. 认证（WS 侧）

- **WS 鉴权**：建立 WS 时带 `RoomToken` 握手校验，校验通过才允许加入 Channel。
- 登录换 token、HTTP 鉴权、防重进等通用认证体系见主文档 [§5.2 认证与安全](./2026-07-24-server-architecture-design.md#52-认证与安全)。

---

## 5. 与主文档数据层的衔接（待落地时细化）

- **同玩家写串行**：实时玩法中触及该玩家数据 key 的写操作，必须同样接入主文档的 **Redisson 分布式锁 `lock:player:{id}`**，与 HTTP 侧写共享同一把锁（否则跨通道并发覆盖会丢更新）。
- **业务日志**：实时玩法中的关键操作（战斗结算发奖、交易等）同样追加主文档定义的**业务侧磁盘日志**（增量 + before/after 绝对值，纯磁盘人工对账依据）。
- **崩溃恢复**：遵循主文档"不自动重放"策略，以 Redis 当前态为准。

---

## 6. 后续待定

- 场景/房间具体模型与路由。
- 各玩法的具体状态机。
- WS 按房间路由的部署形态：实例选择、扩容、灰度/滚动发布下的房间迁移与故障切换。
- 实时玩法写路径接入分布式锁与业务日志的具体接口契约。
- 惊群效应防护（WS 进程崩溃后房间内并发重连的渐进恢复流程）。
