# 集成测试（*IT.java）连接的本地服务配置 —— 模板。
#
# 【使用】把本文件复制成同目录的 application.yaml，改成你本机的实际值：
#     cp application.yaml.example application.yaml
#
# application.yaml 已在 .gitignore 中（含密码，不入库）；本模板则纳入版本控制，
# 作为「有哪些可配项」的说明。新增配置项时记得同步更新本模板。
#
# 仅测试目录，不会打进 jar，与应用的 application.yaml 无 classpath 冲突。
# 改本地环境（换端口、改密码）只改 application.yaml，不必设环境变量，
# 更不要去改生产类 DataProperties 的默认值——那会把本机配置提交进 git，
# 并在部署时忘设环境变量的情况下静默充当线上兜底。
#
# 注意：LocalRedisMongo 每个用例会 flushdb 当前 Redis 库、drop 下面这个 Mongo 库。
# 务必指向本地开发实例，别指向有真实数据的地方。
game:
  data:
    redis-address: redis://127.0.0.1:6379
    # 本机 Redis 的 requirepass；无鉴权则留空
    redis-password: <你的 Redis 密码>
    # 仅 Redis 6+ ACL 需要，一般留空
    redis-username:
    mongo-uri: mongodb://localhost:27017
    mongo-db: game_test
